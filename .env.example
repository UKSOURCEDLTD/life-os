# Optional — cross-device sync via Supabase
# Set up: create a Supabase project, run the SQL from utils/supabase.ts SUPABASE_SCHEMA_SQL constant, then set:
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — AI assistant (food parsing, weekly summaries)
VITE_GEMINI_API_KEY=
